name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: docker.io/zhaoyangkun/aries:latest

    steps:
    - uses: actions/checkout@v4

